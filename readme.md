# Ticktacktoe in Vanilla JS
* Precursor to TOP Ticktacktoe project

## Win condition checking (pseudocode)
### Horizontal
for each row on playfield
    if all items in field are the same mark
        return (mark) wins
    else
        break
